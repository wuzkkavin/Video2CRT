# Video2CRT Tauri App Handoff — 2026-09-15

> **目的**: 給下一個 session 或另一個 agent 接手用。
> **範圍**: Tauri 2 desktop app (`video2crt-app/`) 的當前狀態 + 這次任務做了什麼 + 為什麼 + 還沒做 + 下次怎麼繼續。
> **不要讀這個檔案的場景**: 如果你只是 user 本人在自己電腦繼續操作,直接看 `git log` + 跑 `.exe` 就夠了。

---

## TL;DR

**最後 commit**: `b95e075` on `main` (pushed to `origin/main`)
**最後 build 的 .exe**:
```
C:\Users\asaialabs\Documents\Hermes\Video2CRT\video2crt-app\src-tauri\target\release\video2crt.exe
mtime: 2026-09-15 23:20:40, size: 15,083,008 bytes (15.08 MB)
```
**現在能不能用**: 可以,但有兩個已知缺口(見「未完成」段)。

---

## 1. 環境事實(接手時第一個驗證)

- **OS**: Windows 11
- **Shell**: git-bash / MSYS (POSIX),**不是 PowerShell**
- **User home**: `C:/Users/asaialabs/`
- **Repo root**: `C:/Users/asaialabs/Documents/Hermes/Video2CRT/`
- **Python**: Hermes venv at `C:/Users/asaialabs/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe` (3.11.15)
- **yt-dlp**: 同 venv `Scripts/yt-dlp.exe`
- **ffmpeg**: from PATH (NVIDIA RTX 3050 Ti, hwaccel available)
- **Node**: `C:/Users/asaialabs/AppData/Local/hermes/node/node.exe` (供 yt-dlp `--js-runtimes`)
- **faster-whisper**: `1.2.1`, `medium-multilingual` int8 為預設 model
- **Minimax API**: `https://api.minimax.io/v1`, Bearer token via keyring

驗證:
```bash
ls "/c/Users/asaialabs/AppData/Local/hermes/hermes-agent/venv/Scripts/yt-dlp.exe"
which ffmpeg
"/c/Users/asaialabs/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe" -c "import faster_whisper; print(faster_whisper.__version__)"
```

---

## 2. 程式碼佈局(接手時先看這些)

```
video2crt-app/
├── src-tauri/
│   ├── Cargo.toml              # 含 keyring, chrono, tokio, reqwest, futures
│   ├── tauri.conf.json         # window 760x700, min 640x560
│   ├── capabilities/default.json  # core:default, dialog:default, opener:default
│   ├── src/
│   │   ├── lib.rs              # 7 Tauri commands (start_job, cancel, list_models, ...)
│   │   ├── orchestrator.rs     # 6-stage pipeline (THE core file, ~1300 lines)
│   │   ├── settings.rs         # keyring get/save/has_api_key
│   │   └── translator.rs       # GET /v1/models + fallback list
│   ├── bin/
│   │   └── pipeline_cli.py     # Stage 4-6 Python sidecar (ASR + SRT + burn + mux)
│   └── src/bin/
│       ├── verify_pipeline.rs  # CLI 跑 pipeline bypass Tauri
│       ├── verify_download.rs  # CLI 驗證 yt-dlp spawn
│       ├── verify_sidecar.rs   # CLI 驗證 Python sidecar spawn
│       ├── verify_e2e.rs       # CLI 完整 6-stage e2e
│       └── run_e2e.rs          # 給 verify_e2e 呼叫
└── src/
    ├── App.tsx                 # 4-page reducer state machine
    ├── App.css                 # 暗色主題
    ├── lib/types.ts            # JobOptions + StartJobRequest
    └── pages/
        ├── UrlPage.tsx
        ├── OptionsPage.tsx     # 含字幕/翻譯 toggle、輸出資料夾
        ├── ProgressPage.tsx    # 含時鐘 + LED marquee
        └── DonePage.tsx
```

**進入點**: Tauri 從 `src-tauri/src/lib.rs::start_job` 收 IPC,spawn tokio task → `orchestrator::start` → `orchestrator::run_pipeline` → 6 stages。

---

## 3. 6-Stage Pipeline 怎麼走

```
URL → [1. yt-dlp download] → [2. ffmpeg cropdetect] → [3. ffmpeg + libplacebo CRT] →
[4-6. Python sidecar: faster-whisper ASR + SRT + burn + mux (if enableSubtitles)] →
final.mp4
```

Stage 4-6 是**可選**: `enableSubtitles=false` 跳過,只做 1-3 + raw.mp4 → final.mp4 mux。

**Output 位置**:
```
if req.output_dir is None:
    base = <repo>/output/
else:
    base = req.output_dir

title = fetch_video_title(url, video_id)  # 5s timeout, --no-playlist, fallback yt_<id>
sub_dir = safe_dirname(title, video_id)   # 清 NTFS + shell metas
final_output_dir = base / sub_dir

Files in final_output_dir:
- source.mp4     # yt-dlp 下載的原始
- raw.mp4        # CRT shader 套用後
- zh-Hant.srt    # 雙語字幕(僅 enableSubtitles=true)
- subtitled.mp4  # 燒字幕後(僅 enableSubtitles=true)
- final.mp4      # mux raw.mp4 video + source.mp4 audio, 16:9
```

---

## 4. 這次任務(2026-09-15)做的 8 個 bug 修

### Bug 1: `fetch_video_title` 沒 timeout → pipeline 卡 init
**症狀**: user 貼 `https://www.youtube.com/watch?v=Y2j24BkV4bE&list=RDhBQRY5gajj8&index=5` → 按開始轉檔 → 永遠卡在 `init` 階段,前端進度條不動。
**根因**(看 startup.log):
```
[trace] A: about to create_dir_all(...)
[trace] A: about to create_dir_all(...)  # 之後就停了,沒有 B0 也沒有 B0.5
```
`fetch_video_title()` spawn yt-dlp 抓 metadata,user 的 URL 含 `list=RD...&start_radio=1`(YouTube 的 Mix playlist),yt-dlp 預設展開整個 playlist,永遠跑不完 → `await fetch_video_title()` 永遠不回。
**修**(`orchestrator.rs:914-984`):
- 整個 title-fetch 包進 `tokio::time::timeout(Duration::from_secs(5), ...)`
- 加 `--no-playlist` 不展開 playlist
- Timeout 或 decode 失敗 fallback `yt_<id>`,pipeline 不卡死

### Bug 2: `[Errno 22] Invalid argument` yt-dlp 下載失敗
**症狀**: 含特殊字元的影片標題(如 `AI Art... "Creator or Critic" & Crystal, 4K`)下載失敗。
**根因**(看 startup.log):
```
output_dir = C:\Users\asaialabs\Desktop\AI_Art_Steampunk_Video_"Creator_or_Critic"_Chris_&_Crystal_AI_Art_Original_Music,_4K
ERROR: Unable to download video: [Errno 22] Invalid argument
```
`safe_dirname()` 只清 NTFS-illegal chars (`<>:"/\|?*`),但**沒清 Windows shell metacharacters** (`& , ; ' \` $ ( ) { }`)。yt-dlp `-o` template 把這些 path 進 CreateProcess → `CommandLineToArgvW` 解析失敗 → `OSError [Errno 22]`。
**修**(`orchestrator.rs:850-883`): 加清 shell metas。

### Bug 3: Python sidecar stdout cp950 flush 失敗 → 整個 pipeline 顯示「asr failed」
**症狀**: log 顯示 `mux complete` 後突然 `asr failed: cp950 UnicodeEncodeError`。
**根因**: `pipeline_cli.py` 跑在 cp950(繁中 Windows),把 `×` `'` 等 UTF-8 字元塞 stdout → flush 失敗。**但 `final.mp4` 已成功寫好**。
**修**(`orchestrator.rs:553-559`):
```rust
cmd.env("PYTHONIOENCODING", "utf-8");
cmd.env("PYTHONUTF8", "1");
```
**注意**: 這只對 Python 端有用,**不要**對 yt-dlp spawn 加 — yt-dlp 的 sys.stdout 已經綁 cp950,加 env var 反而讓 yt-dlp 在第一個 `×` 上炸。

### Bug 4: 輸出資料匣亂碼 `×` 變 `?`
**症狀**: `NYC_Surreal_AI_Art_Video_×_Don't_Fit_In×_Chris_&_Crystal_AI_Art_×_4K` 的 `×` `'` 顯示成 `?`。
**根因**: 從 #3 反向追:`fetch_video_title` 用 `String::from_utf8_lossy(&out.stdout)` 解碼,invalid bytes 被替換成 U+FFFD → `safe_dirname` 保留下來 → NTFS 把 U+FFFD 顯示成 `?`。
**修**(`orchestrator.rs:968-975`): 用 `std::str::from_utf8` 嚴格 decode,失敗 fallback `yt_<id>`。

### Bug 5: 按「開始轉檔」按鈕重複點 → 13 個 pipeline 搶同一個 output_dir
**症狀**: user log 顯示按 13 次開始轉檔(短時間內),13 個 pipeline 同時跑,搶同一個 source.mp4,GPU 也搶。
**根因**: `handleStart` 直接呼叫 `onStart`,沒有 re-entrancy guard。
**修**(`OptionsPage.tsx:49-72`):
```typescript
const [starting, setStarting] = useState(false);
const handleStart = () => {
  if (starting) return;
  setStarting(true);
  onStart({...});
};
// button:
<button disabled={starting} style={starting ? { opacity: 0.5, cursor: "not-allowed" } : undefined}>
  {starting ? "轉檔中…" : "開始轉檔 →"}
</button>
```

### Bug 6: 純音樂跑 ASR 卡 30 分鐘
**症狀**: user 貼 `vdBeuLGZ1UE`(純音樂)→ 跑到 67% ASR 卡 30 分鐘。
**根因**: `enableSubtitles` 預設 true,但純音樂沒人聲,faster-whisper `medium-multilingual` 跑完找不到 segments 又去跑 chunked ASR。
**修**(`orchestrator.rs:399-460`): 加 `enable_subtitles` flag,`false` 直接跳 Stage 4-6,只做 `下載 → cropdetect → CRT → mux → final.mp4`。

### Bug 7: Python sidecar 不吐 log 時「取消」無效
**症狀**: 按取消,UI 沒反應,要全關掉 .exe。
**根因**: `run_sidecar` 用 `BufReader::read_until(b'\n')` 收 stdout,faster-whisper 跑整段不吐 log → cancel flag 只在新 line 才檢查,永遠等不到。
**修**(`orchestrator.rs:591-...`): spawn 一個 `tokio::spawn` watcher 每 200ms 輪詢 cancel_flag,觸發就 `child.kill()`。

### Bug 8: 字幕背景色
**狀態**: **未完成**。User 在 2026-09-14 提「字幕背景色跟軟體同色(太空黑)」,這次只改了 LED 跑馬燈背景,**字幕 SRT 燒錄的背景色沒改**。

---

## 5. UI / UX 改動

| 檔案 | 改動 | 為什麼 |
|---|---|---|
| `OptionsPage.tsx` | 新增 `☑ 產生字幕` + `☐ 啟用雲端翻譯` checkbox + 翻譯模型 select | User 想分開控制 ASR 跟翻譯 |
| `OptionsPage.tsx` | 「輸出資料夾」欄位 + 「瀏覽…」按鈕(用 `@tauri-apps/plugin-dialog` open) | User 想自訂輸出位置 |
| `OptionsPage.tsx` | 按鈕 disable lock + 「轉檔中…」文字 | 見 Bug 5 |
| `ProgressPage.tsx` | `useState now` + `setInterval(1s)`,顯示「本地時鐘 + 已等待」在 log region 下方 | 不再盲等 |
| `ProgressPage.tsx` + `App.css` | LED 跑馬燈 `(c) copyright 2026 by WUZK` 取代舊靜態文字 | 太空黑 transparent 背景、無上下兩條線、彩色、雙段文字無縫滾動 |
| `App.css` | `.options-page overflow: hidden → overflow-y: auto` | 760×700 預設視窗可直接捲到「開始轉檔」 |
| `App.css` | `.input`, `.select`, `.options-url-preview`, `.checkbox-row` 加 `min-height: 38px` | 欄位大小統一 |
| `App.css` | `.field margin-bottom: 14px → 10px` | 6 個欄位時不會被切 |
| `lib.rs` | `StartJobRequest` 加 `output_dir?: Option<String>` | 配 `OptionsPage` 輸出資料夾 |
| `lib/types.ts` | `JobOptions` 加 `enableSubtitles: boolean, cloudTranslation: boolean, translationModel?: string \| null` | 配 UI toggle |
| `App.tsx` | `DEFAULT_OPTIONS` 預設 `enableSubtitles=true, cloudTranslation=false, translationModel=''` | 預設行為 |
| `App.tsx` | `startJob` payload 把新選項送給 Rust | |
| `App.tsx` | reducer `PROGRESS` case 改為 `logLine OR message` 都 append to logs | 修 Bug:之前 `logLine` 為空時 log 不更新 |

---

## 6. Gotcha(接手時不要再踩)

- **`PYTHONIOENCODING=utf-8` 只對 Python 端有用,不要對 yt-dlp spawn 加**。yt-dlp 的 sys.stdout 在 spawn 時已綁,加 env var 會讓 yt-dlp 在第一個 non-cp950 byte 上炸。
- **`safe_dirname` 要清 `& , ; ' \` $ ( ) { }`** —— 不只 NTFS-illegal。Windows shell metacharacters 也會讓 yt-dlp `-o` 炸 Errno 22。
- **fetch_video_title 一定要有 timeout**。User 貼 `list=RD...` 的 playlist URL 是常態,沒 timeout pipeline 永遠卡 init。
- **`--no-playlist` 不要拿掉**。同樣理由。
- **不要在 OptionsPage 拿掉 `starting` lock**。沒鎖 user 按一次 → 13 個 pipeline 同時跑 → source.mp4 互相覆蓋。
- **stage 4-6 跳過路徑(enableSubtitles=false)** 仍要做 `mux audio + -aspect 16:9`。不能只跳 ASR 就給 user 一個 raw.mp4。
- **Windows `CREATE_NO_WINDOW` (0x08000000)** 必須加在所有 Rust `Command::new`,否則 explorer-launch 會跳黑色終端。
- **Hermes venv python 必須用絕對路徑 + PYTHONPATH**。`.exe` 從 explorer launch 沒繼承 venv PATH。
- **`yt-dlp --js-runtimes node:<abs path>`** —— YouTube JS challenge 需要 node runtime。
- **`--remote-components ejs:github`** —— yt-dlp 需要從 GitHub 抓 JS extraction rules。
- **emit_empty_translation=True** 永遠保留 ASR verbatim line 1。演講影片沒歌詞字典也要有原文字幕(gotcha 11)。

---

## 7. 未完成 / 已知缺口

| 項目 | 狀態 | 為什麼沒做 |
|---|---|---|
| 字幕背景色 = 太空黑 | 未做 | 這次只改了 LED marquee,字幕燒錄的 background color 仍是預設黑底 |
| Phase 2 portable bundling | 未做 | 自動裝 yt-dlp.exe + ffmpeg.exe + python.exe embed 到 `%LOCALAPPDATA%\Video2CRT\bin\` + first-run wizard。User 2026-09-13 提「要能散佈給其他電腦使用」 |
| 純音樂路徑端到端驗證 | 未做 | 沒實際跑一支純音樂 .exe 跑完最後 stage_done。Bug 6 修法只看 code 對 |
| Memory 改寫 | 未做 | User memory 已 98% 滿 (2156/2200)。這次的所有細節寫進這個 handoff 檔,**不要再 add memory** |
| `output/yt_*/` 髒資料 | 故意沒動 | User 自己的 dirty working tree,commit 時 `git add` 只挑任務改動的檔 |
| `HANDOFF-newtask-app.md` 改寫 | 未做 | User 的個人筆記,跟任務 handoff 分開 |

---

## 8. 下次接手第一步

```bash
cd "/c/Users/asaialabs/Documents/Hermes/Video2CRT"

# 1. 確認 build 過
git status
git log --oneline -5

# 2. 確認 .exe 是新版本
stat "video2crt-app/src-tauri/target/release/video2crt.exe"
# 期望: mtime >= 2026-09-15 23:20:40, size ~15083008

# 3. 跑 self-verify(確認 spawn 邏輯沒壞)
"video2crt-app/src-tauri/target/release/verify_download.exe" \
  "https://www.youtube.com/watch?v=jNQXAC9IVRw" \
  "/c/Users/asaialabs/Documents/Hermes/Video2CRT/output/self-verify/"
# 期望: download ~14s, file size > 1MB

# 4. 跑 self-verify sidecar
"video2crt-app/src-tauri/target/release/verify_sidecar.exe"
# 期望: spawn python + pipeline_cli.py, 印 sidecar log

# 5. 如果 user 還想驗證字幕背景色:編輯 pipeline_cli.py
#    找 build_bilingual_srt / srt_to_ass 那段,把 bg color 改 &H00000000&
#    或改用 ASS drawtext + BackgroundStyle 或 libass fill color

# 6. 如果 user 還想驗證純音樂路徑:用 vdBeuLGZ1UE,enableSubtitles=false
```

---

## 9. Commit 記錄

```
b95e075 (HEAD -> main, origin/main) feat(app): video title as output subdir + shell-meta-safe path + enableSubtitles toggle + local clock + LED marquee + cancel-button lock
9d830a2 fix(app): yt-dlp with --js-runtimes node + --remote-components ejs:github
dd0523f fix(app): yt-dlp --no-playlist to handle URLs with start_radio=1 etc.
3e0004a fix(app): from_utf8_lossy on sidecar stdout instead of strict lines()
a5a14c3 fix(app): locate Python sidecar via Hermes venv absolute path
994962b fix(app): user output dir + CREATE_NO_WINDOW + locate_sidecar + wall-clock log ts
86ffcdc fix(app): yt-dlp candidate lookup drops PATH probe, uses absolute path only
7958f73 fix(app): local timestamp + yt-dlp Hermes venv fallback + UI height 38px + window 760x700
```

---

## 10. log 位置(debug 第一個看這)

```
C:\Users\asaialabs\Documents\Hermes\Video2CRT\video2crt-startup.log
```
IPC 觸發時寫入。Pattern:
- `[trace] A: ...` → `[trace] B0: ...` → `[trace] B0.5: ...` 是同步起點
- `[trace] B: create_dir_all OK` 之後是 tokio::spawn 入口
- `[emit] stage=...` 是 progress 事件
- `[stage] tokio::spawn task entered for <video_id>` 是真正 pipeline 開始

如果 pipeline 卡 `[trace] A` 沒進 `B0`,就是 `fetch_video_title` 在 await 沒回來(看 Bug 1)。
如果卡 `[trace] B: create_dir_all OK` 之後沒 `[stage]`,就是 tokio::spawn panic 了(看 main.rs panic catch)。

---

## 11. 已知不相關的問題(接手時不要修)

- **Cargo 編譯 lint 一直報 `async fn is not permitted in Rust 2015`** —— 是 lint 誤報,實際 cargo build 成功(看 `Finished release profile`)。編輯 `Cargo.toml` 的 `edition` 不影響,別浪費時間。
- **Vite 1.7s + Cargo 2-3 min** —— 增量 cache 正常,改個小東西不用等冷編譯。
- **`output/yt_*/` 36 個刪除的 SRT/JSON** —— user 的 dirty working tree,**不要 commit**。

---

## 12. 設計決策(為什麼這樣做)

- **Stage 1-3 Rust 直接 spawn ffmpeg/yt-dlp**: 效能、可控、不要 maintain Rust ASR binding
- **Stage 4-6 Python sidecar (`pipeline_cli.py`)**: 複用現有 `src/video2crt/` ASR + SRT 模組,不用 Rust 重寫 faster-whisper
- **API key 存 Windows Credential Manager**(keyring crate): service="Video2CRT", user="default",**不要寫進 `.env`**(user 之前要求)
- **fetch_video_title 是 best-effort**: 5s timeout,失敗 fallback `yt_<id>`,pipeline 不會因為標題抓不到而壞掉
- **safe_dirname 清 shell metas**: 寧可犧牲可讀性(`AI_Art_Steampunk_Video_Creator_or_Critic_...`),也不要 Errno 22
- **OptionsPage 鎖按鈕 vs JobRegistry 在 Rust 鎖**: 前端鎖 UX 友善,Rust 鎖要 IPC 來回,延遲較高。兩者都做但前端鎖 user 看得到
- **訊息模式**: 看 startup.log + run verify_*.exe CLI,**不要靠 user 回報**。User 鐵律「你重新 debug 驗證」

---

如果接手時這份文件跟程式碼對不上,**以程式碼為準**,更新這個檔。
